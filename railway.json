{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install && npm install && npm run build && php artisan migrate --force",
    "nixpacksConfig": {
      "phpExtensions": ["gd"]
    }
  }
}