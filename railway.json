{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --optimize-autoloader --no-interaction && php artisan migrate --force",
    "nixpacksConfig": {
      "phpExtensions": ["gd", "pdo_mysql", "mbstring", "zip", "xml", "curl"]
    }
  }
}